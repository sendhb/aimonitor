#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
autoloop — Python 版 autoloop 启动器薄入口（TASK-025，迁移设计 TASK-090）

实现位于 kit/cli/lib/autoloop_launcher.py（可 import / 可单独运行）；
本文件只做 sys.path 接线 + 入口分发，方便 `python kit/cli/autoloop coder` 调用。

用法（与旧 Bash 版一致）:
  python kit/cli/autoloop coder                # 启动 coder（默认 --once --llm pi）
  python kit/cli/autoloop reviewer             # 启动 reviewer（默认 --once --llm pi）
  python kit/cli/autoloop both                 # 默认后台常驻：coder → reviewer 顺序
  python kit/cli/autoloop both --once          # 前台单轮
  python kit/cli/autoloop both --foreground    # 前台常驻（Ctrl-C 停止）
  python kit/cli/autoloop stop                 # 停止本项目常驻 both 实例
  python kit/cli/autoloop status               # 单屏聚合：壳死活/LLM 子进程/in-progress/最近事件
  python kit/cli/autoloop ensure               # 看门狗：幂等探活+拉起（供定时器高频调用，TASK-107）
  python kit/cli/autoloop coder --interval 300 # 常驻轮询（覆盖默认单轮）

注意: 本文件是 Python 3 脚本（与 cli/task 一致），用 `python kit/cli/autoloop`
调用（Windows 无 python3；Linux 也可 ./kit/cli/autoloop）；**不要用 bash
kit/cli/autoloop**（bash 会把 Python 源码当 shell 逐行解析，产生巨量输出并
死循环，脚本已自带防护）。
"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "lib"))
from autoloop_launcher import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
