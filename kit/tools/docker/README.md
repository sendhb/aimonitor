# tools/docker

> 见 [tools/README.md](../README.md)

> **处置（TASK-098）**：由宿主 CLI 承担——docker run/exec/build 经 shell 能力调用即可；
> 无网络隔离场景已由 `cli/sandbox-run` 覆盖（见 [sandbox/](../sandbox/README.md)）。
> 专项容器编排（多容器环境管理）远期按需立项。
