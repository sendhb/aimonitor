---
description: 对话人格指路 — 进入会话先 ensure，缺失随机激活
globs: **/*
---
# 对话人格（强制激活 · 按需加载 · 表达层设定）

**进入任何 AI CLI 会话时，先运行 `kit/cli/persona ensure`**：
- `personas/active.md` 存在 → 保持当前人格（不变更）；
- 缺失（未激活）→ 自动从人格库随机激活一个。

切换: `kit/cli/persona list | use <name> | off | show`。
当前激活人格见项目根 `personas/active.md`。

本文件为薄壳指路，不复制内容，避免双源漂移。
